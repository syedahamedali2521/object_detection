
# Neon YOLOv8 Object Detector (Image + Video)

A Streamlit app with a futuristic neon UI that performs object detection on images and videos using YOLOv8n. The model downloads at runtime on first use.

## Run locally
```bash
pip install -r requirements.txt
streamlit run app/app.py
```
